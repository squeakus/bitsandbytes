%% ZVal
% Finds the height directly above a point
%% Input
% a point and a plane
%% Returns
% Coordinate
%% Example:
% testfunc(1,2,3)
%%

function [z]=testfunc(a,b,c)
    z = a + b + c;
end
